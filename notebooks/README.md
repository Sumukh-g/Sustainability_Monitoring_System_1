# Reproducible notebook workflow

The operational system is module-first. The numbered notebooks are lightweight research entry points that import those modules rather than duplicating production logic. Execute them after data generation and model training; calculated tables and figures remain under `reports/`.
